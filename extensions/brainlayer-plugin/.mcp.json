{
  "_comment": "Requires BrainBar running and owning /tmp/brainbar.sock. Prefer socat; brainlayer setup rewrites bridge/bun/brainlayer-mcp leftovers to this form.",
  "mcpServers": {
    "brainlayer": {
      "command": "socat",
      "args": ["STDIO", "UNIX-CONNECT:/tmp/brainbar.sock"]
    }
  }
}
